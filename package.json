{
  "name": "gta-vi-map",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "GTA VI Interactive Map — Leonida location guide (pre-launch countdown)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "pages:build": "vite build && node scripts/sync-docs.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  }
}